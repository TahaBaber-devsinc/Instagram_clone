$(function () {
  let comment_id = <%= @comment.id %>;
  $("#like-comment-btn-"+comment_id).show();
  $("#dislike-comment-btn-"+comment_id).css("display", "none");
  $("#comment-like-count-"+comment_id).text("<%= @comment.likes.count %>");
});

